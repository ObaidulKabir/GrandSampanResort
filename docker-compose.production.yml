name: grand-sampan-production

services:
  frontend:
    build:
      context: ./frontend
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-https://www.grandsampanresort.com}
        # Internal rewrite target for /uploads → backend (never the public CF URL).
        - API_URL=http://grand-sampan-prod-backend:4000
      target: production
    container_name: grand-sampan-prod-frontend
    restart: unless-stopped
    environment:
      # Server-rendered pages call the backend over the internal Docker
      # network instead of hairpinning back out through the public domain.
      - API_URL=http://grand-sampan-prod-backend:4000
    networks:
      - prod-network
    ports:
      - "${FRONTEND_PORT:-3005}:3000"

  backend:
    build:
      context: ./backend
      target: production
    container_name: grand-sampan-prod-backend
    restart: unless-stopped
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET}
      # Bootstraps the first admin account on startup; only created if this
      # email doesn't already exist, so it's safe to leave set permanently.
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - ADMIN_NAME=${ADMIN_NAME}
      - PUBLIC_SITE_URL=${PUBLIC_SITE_URL:-https://www.grandsampanresort.com}
      - SMTP_HOST=${SMTP_HOST:-smtppro.zoho.com}
      - SMTP_PORT=${SMTP_PORT:-465}
      - SMTP_SECURE=${SMTP_SECURE:-true}
      - SMTP_USER=${SMTP_USER:-admin@grandsampanresort.com}
      - SMTP_PASS=${SMTP_PASS}
      - MAIL_FROM=${MAIL_FROM:-Grand Sampan Resort <admin@grandsampanresort.com>}
      - MAIL_BCC=${MAIL_BCC:-admin@grandsampanresort.com}
    ports:
      - "${BACKEND_PORT:-4005}:4000"
    volumes:
      # Persist uploaded media across container rebuilds/redeploys.
      - prod_uploads_data:/app/uploads
    networks:
      - prod-network
    depends_on:
      primary-db:
        condition: service_healthy

  primary-db:
    image: postgres:17-alpine
    container_name: grand-sampan-prod-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-grand_sampan}
    volumes:
      - prod_pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    ports:
      - "${POSTGRES_PORT_HOST:-5432}:5432"
    networks:
      - prod-network

networks:
  prod-network:
    name: grand-sampan-prod-network
    driver: bridge

volumes:
  prod_uploads_data:
    name: grand_sampan_prod_uploads_data
  prod_pgdata:
    name: grand_sampan_prod_pgdata
